alter session set current_schema = shopping;
SET heading OFF;
SELECT FIRST_NAME, CITY FROM CUSTOMERS WHERE CUSTOMER_ID = 
(SELECT CUSTOMER_ID FROM ORDERS GROUP BY CUSTOMER_ID HAVING COUNT(*) = 
(SELECT MAX(COUNT(*)) FROM ORDERS GROUP BY CUSTOMER_ID));
