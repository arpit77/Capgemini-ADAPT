alter session set current_schema = shopping;
SET heading OFF;
SELECT * FROM PRODUCTS WHERE PRODUCT_ID IN 
 (SELECT PRODUCT_ID FROM ORDER_ITEMS GROUP BY PRODUCT_ID HAVING 
 COUNT(*) = (SELECT MAX(COUNT(*)) FROM ORDER_ITEMS GROUP BY ORDER_ID));
