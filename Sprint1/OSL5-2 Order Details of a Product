alter session set current_schema = shopping;
SET heading OFF;
SELECT * FROM ORDERS WHERE ORDER_ID IN 
(SELECT ORDER_ID FROM ORDER_ITEMS WHERE PRODUCT_ID IN 
(SELECT PRODUCT_ID FROM PRODUCTS WHERE TRIM(PRODUCT_NAME) = 'Canon 200D'));
