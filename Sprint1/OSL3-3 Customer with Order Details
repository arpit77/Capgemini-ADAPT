alter session set current_schema = shopping;
SET heading OFF;
 SELECT CUSTOMERS.FIRST_NAME, CUSTOMERS.CITY, ORDERS.ORDER_DATE FROM CUSTOMERS 
 INNER JOIN ORDERS ON ORDERS.CUSTOMER_ID = CUSTOMERS.CUSTOMER_ID;
